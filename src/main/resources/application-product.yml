server:
  port: 8183
spring:
  config:
    activate:
      on-profile: product
openapi:
  service:
    api-docs: api-service
    server: http://localhost:${server.port}
    title: API Service
    version: 1.0.0
